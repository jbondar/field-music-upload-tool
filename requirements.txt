fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
itsdangerous==2.2.0
python-multipart==0.0.20
# grants_events is NOT installed here -- it's a private-repo dependency
# (web-services) and a plain requirements.txt line has nowhere to put the
# credential that needs. See the Dockerfile's own RUN step: it's installed
# separately, via a BuildKit secret mount, only when one is provided.
# app/main.py's import of it is defensive for exactly that reason.
